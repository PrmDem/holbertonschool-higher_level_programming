#!/usr/bin/python3

def print_list_integer(my_list=[]):
    # prints integers stored in a list
    # takes 'my_list' as an argument
    # doesn't return anything
    for item in my_list:
        print("{}".format(item))
